#single node install config
coordinator=true
node-scheduler.include-coordinator=true
http-server.http.port=8080
query.max-memory=1GB 
query.max-memory-per-node=512MB
discovery-server.enabled=true
discovery.uri=http://localhost:8080
# discovery.uri=https://localhost:8443
catalog.management=${ENV:CATALOG_MANAGEMENT}

http-server.authentication.type=PASSWORD

# PASSWORD 认证默认拒绝明文 HTTP（返回 401 "Password not allowed for
# insecure authentication"）。本地开发只暴露在 dev_db_network 和 localhost，
# 未配 TLS（setup-certs.sh 从未执行，无 trino.jks），故显式放行 HTTP。
# 要改用 TLS：跑 setup-certs.sh，启用 http-server.https.*，再删掉这一行。
http-server.authentication.allow-insecure-over-http=true

# 当开启认证后，必须配置此项，用于节点间通信签名
internal-communication.shared-secret=32e5b096d21ba2b43c932545301626d2

