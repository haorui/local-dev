mode:
  type: Cluster
  repository:
    type: ZooKeeper
    props:
      namespace: governance_ds
      server-lists: shardingsphere_zookeeper:2181
      retryIntervalMilliseconds: 500
      timeToLiveSeconds: 60
      maxRetries: 3
      operationTimeoutMilliseconds: 500

authority:
  privilege:
    type: SMARTDATA
  users:
    - user: __Ss__root@%
      password: 8413dbb649e44792c01094cfe07c0e11
      admin: true

props:
    proxy-mysql-default-version: 8.0.25

# NOTE: ShardingAuditAlgorithm only works with sharded tables.
# For global permission checking, we need a different approach.
# Options being investigated:
# 1. Use ShardingSphere Agent with custom plugin
# 2. Implement custom AuthenticationProvider
# 3. Use SQL Parser Rule for blocking specific SQL patterns

