port 7003
cluster-enabled yes
# 集群配置文件（節點會自動生成並更新）
cluster-config-file nodes.conf
cluster-node-timeout 5000
# 啟用 AOF 持久化
appendonly yes
protected-mode no

#logfile "/var/log/redis.log"
#dir "/data"

# 節點密碼 (選擇性配置)
# 設置訪問密碼
requirepass "Zonesec2024."
# 主從節點之間的認證密碼
masterauth "Zonesec2024."         
