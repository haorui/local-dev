## Sentinel适用场景

Sentinel 模式

適合中型應用場景，提供高可用的 Redis 集群，但並不具備數據分片功能。

    •	使用場景：適用於對數據容量需求有限，但需要高可用性的場景。如中小型應用，重要的單一資料庫應用等。
    •	架構：通過主從（master-slave）結構提升可用性，Sentinel 节点监控 Redis 的主节点，并在主节点故障时将从节点提升为主节点。
    •	優點：具備高可用性，能在 Redis 主節點故障時自動故障轉移到從節點。
    •	缺點：不具備數據分片能力，數據量受限於單節點最大容量，且可能會有少量延遲。
